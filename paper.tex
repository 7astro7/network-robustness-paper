\documentclass[12pt]{article}
\usepackage[a4paper, margin=1in]{geometry}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{float}
\usepackage{placeins}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{hyperref}
\hypersetup{
    colorlinks=false
}
\usepackage{nameref}
\usepackage{physics}
\usepackage{siunitx}
\usepackage{lmodern}
\usepackage{enumitem}
\providecommand{\keywords}[1]{\noindent\textbf{Keywords:} #1\par\vspace{0.5em}}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\title{Entropy-Based Indicators of Critical Transitions in
Power-Law Networks Under Progressive Node Removal}
\author{Zachary Kraehling\thanks{Correspondence: \texttt{zak@zakkraehling.net}. This research received no specific grant from any funding agency, commercial or not-for-profit sector.}\\
\small Independent Researcher, Nashville, TN, USA\\
\small 1801 Hayes St, Nashville, TN 37203, USA}
\date{2026}

\newcommand{\CAIDASnapshot}{2026-01-01}

\begin{document}

\maketitle

\begin{abstract}
Power-law networks tolerate extensive random failures yet collapse abruptly under targeted attacks with little warning from connectivity measures alone. We track smoothed successive Kullback--Leibler (KL) divergence between adjacent empirical degree distributions in Chung--Lu (CL) graphs ($2<\gamma<3$) under progressive node removal using fixed detection rules that do not reference connectivity. Under random failure the signal rises above the calibration-window threshold before giant-component collapse (40/40 seeds at $\gamma=2.5$; replicated on an empirical Internet AS topology); under targeted removal it exhibits immediate disruption. The residual degree distribution is derived analytically via binomial thinning: a Fisher information approximation explains why the KL signal accelerates near the percolation threshold. Null and low-damage controls confirm specificity (0/40 false triggers in each). Successive KL produces earlier warning than rolling-variance early-warning signals under the same decision rule, reflecting rolling-window lag inherent to those alternatives; the Fisher information $\mathcal{I}(q)$ of the analytical residual distribution grows near $q_c$, confirming signal acceleration near the percolation threshold. The indicator's divergence between CL and the configuration model (CM) reveals its sensitivity to stub-pairing-induced structural correlations: the baseline KL noise floor is elevated in CM relative to CL ($\bar{\mu}_0$ by 1.4--3.8$\times$, $\bar{\sigma}_0$ by 2.0--5.5$\times$ across $\gamma$), inflating the detection threshold above the post-window signal in the majority of CM runs.
\end{abstract}

\keywords{network robustness, early-warning signals, KL divergence, power-law networks, percolation}

\section{Introduction}
Many real-world networks exhibit degree distributions of the form $P(k)\propto k^{-\gamma}$
with $\gamma\in(2,3)$~\citep{newman2003structure,barabasi1999scaling}, reflecting extreme
heterogeneity and the presence of high-degree hubs. Network robustness is traditionally
assessed via giant connected component (GCC) fraction $S(q)$ under node removal, which provides limited insight into
how structure degrades before fragmentation. Robustness transitions often occur abruptly
with little warning from classical connectivity indicators~\citep{radicchi2024robustness}.
\citet{suweis2014earlywarning} show that rising variance and
cross-correlation anticipate instability in node-state dynamics on networks; our setting
differs in that we monitor structural degradation under node removal with no node-state
time series, seeking early warning from topological reorganization via successive KL
divergence between degree distributions~\citep{scheffer2009earlywarning,dakos2012methods}.

Information-theoretic quantities have characterized static graph structure but not progressive
damage. \citet{dehmer2008information} introduced Shannon-entropy measures for static
graph complexity; none connect entropy to node failures, percolation, or GCC collapse.
Robustness literature~\citep{albert2000error,cohen2000resilience,radicchi2024robustness}
relies on degree moments, spectral radius, or percolation thresholds, not on
information-theoretic indicators that change before collapse. KL divergence between successive degree distributions captures rates of structural
reorganization; the Fisher information of the residual distribution explains why the signal accelerates
near $q_c$. We ask whether such rates provide early-warning signals of impending
connectivity collapse in power-law networks under progressive node removal, and under what
conditions the signal transfers across generative ensembles with matched tail exponents.

Unlike entropy, which summarizes structure at a fixed damage level, successive KL divergence
measures rates of change; being sensitive to the full distributional shape rather than
low-order moments, it detects connectivity shifts before they appear macroscopically.
We use exponentially weighted moving average (EWMA)-smoothed successive KL divergence to detect the onset of nonstationary
structural reorganization under progressive node removal, using fixed decision rules that
do not reference connectivity; we do not estimate $q_c$, and in the targeted regime the
signal is interpreted as an immediate-disruption intensity summary rather than advance warning.
Across 40 independent seeds per $\gamma$ over nine values in $(2,3)$, the signal departs
from the calibration-window baseline at $q_{\mathrm{warn}} \ll q_{\mathrm{collapse}}$ in all or nearly
all seeds at every tested $\gamma$ (e.g., 40/40 at $\gamma=2.5$, median lead time
$\Delta_{\mathrm{warn}} \approx 0.55$), reproduced on a Center for Applied Internet Data Analysis (CAIDA) Autonomous System (AS) graph (40/40 seeds).
The residual degree distribution under random removal is derived analytically via binomial
thinning (Eq.~\ref{eq:binomial_thinning}), connecting $\tilde{P}(k;\,q)$ to the
Molloy--Reed threshold; a Fisher information approximation then shows that successive KL
per step is proportional to $\mathcal{I}(q)$, which grows as the high-degree tail contracts
near $q_c$, explaining the pre-collapse signal acceleration; empirically, the
baseline-deviation rule applied directly to successive KL produces earlier warning than
rolling-variance EWS under the same decision rule, a timing advantage consistent with the
accelerating $\mathcal{I}(q)$.
Configuration-model experiments reveal that the indicator diverges between Chung--Lu and
configuration-model ensembles, highlighting its sensitivity to stub-pairing-induced structural
correlations that soft-constraint wiring does not introduce; the hard-constraint wiring
mechanism is an independent structural channel of ensemble sensitivity.

\section{Related Work}

Early-warning signals for tipping points have been studied extensively in ecological and climate systems, where rising variance and autocorrelation precede critical transitions \citep{scheffer2009earlywarning,scheffer2012anticipating}. Critical slowing down, manifesting as increasing lag-1 autocorrelation, is a widely used indicator in time-series settings \citep{vannes2007slow,dakos2012methods}. Recent work extends EWS to machine-learning-based detection \citep{bury2021deeplearning} and to structural disintegration in complex systems via network-feature monitoring \citep{grassia2021machine}, as well as to percolation-based signals in coupled oscillatory systems \citep{ehstand2023network}. Our work differs in that we monitor structural degradation of a static network under progressive node removal rather than node-state dynamics on a fixed graph, and we employ a fully analytical connection between the indicator and Fisher information.

Network resilience and robustness have been characterized analytically via degree-moment conditions \citep{molloy1995critical,callaway2000network} and percolation theory \citep{cohen2000resilience}. \citet{gao2016resilience} show that high-dimensional network dynamics can be reduced to a one-dimensional system, enabling resilience prediction from network structure; their framework targets dynamical resilience rather than structural early warning under damage. \citet{suweis2014earlywarning} demonstrate that rising variance and cross-correlation in node-state dynamics anticipate instability on networks, but rely on a time series of node states rather than the evolving degree distribution. The robustness literature \citep{albert2000error,radicchi2024robustness} relies primarily on GCC fraction and degree-moment thresholds, not information-theoretic quantities that change before collapse. Information-theoretic measures of static graph complexity were introduced by \citet{dehmer2008information} but do not address progressive damage or percolation. The present work applies successive KL divergence to the sequence of empirical degree distributions under node removal, connecting distributional change rates to the Fisher information of the binomial-thinning residual distribution.

\section{Methods}

\subsection{Problem Formulation}

Let $G_0$ have degree distribution $P_0(k)$. Progressive removal of fraction $q$ produces
damaged graph $G_q$ with distribution $P_q(k)$. We track the successive KL divergence:
\[
D_{\mathrm{KL}}(P_{q+\Delta q} \| P_q) = \sum_k P_{q+\Delta q}(k) \log_2 \frac{P_{q+\Delta
q}(k)}{P_q(k)}.
\]

We evaluate $D_{\mathrm{KL}}$ as a per-step reorganization proxy, tracking warning timing
$q_{\mathrm{warn}}$, lead time $\Delta_{\mathrm{warn}}=q_{\mathrm{collapse}}-q_{\mathrm{warn}}$
(where $q_{\mathrm{collapse}}$ is defined by $S(q)<0.1$), and the robustness of these
statistics across the $\gamma$ sweep.


\subsection{Theoretical Background}

For uncorrelated random graphs with degree moments $\langle k \rangle$ and
$\langle k^2 \rangle$, the Molloy--Reed criterion~\citep{molloy1995critical,molloy1998size}
for GCC existence is
\[
\kappa = \frac{\langle k^2 \rangle - \langle k \rangle}{\langle k \rangle} > 1.
\]
Under independent random node failures with probability $q$, this gives
\[
(1 - q)\kappa = 1
\qquad \Rightarrow \qquad
q_c = 1 - \frac{\langle k \rangle}{\langle k^2 \rangle - \langle k \rangle}.
\]

Classical percolation theory thus characterizes collapse via low-order degree moments~\citep{dorogovtsev2008critical}.

Under random node removal, each surviving node of original degree $k$ retains each edge with probability $(1-q)$;
residual degree is Binomial$(k,\,1{-}q)$, giving
\begin{equation}
\tilde{P}(k';\,q)
\;=\;
\sum_{k \ge k'} P_0(k)\,\binom{k}{k'}(1-q)^{k'}\,q^{k-k'}.
\label{eq:binomial_thinning}
\end{equation}
Equation~\ref{eq:binomial_thinning} is exact for locally tree-like graphs in the infinite-size limit~\citep{callaway2000network,cohen2000resilience} and provides an accurate approximation for finite Chung--Lu graphs at moderate damage levels.

The moments of $\tilde{P}$ satisfy
\begin{equation}
\langle k' \rangle = (1-q)\langle k \rangle_0,
\qquad
\langle k'^2 \rangle = (1-q)^2\langle k^2 \rangle_0 + (1-q)q\langle k \rangle_0,
\label{eq:thinned_moments}
\end{equation}
from which the Molloy--Reed branching factor under damage is
\[
\kappa(q)
= \frac{\langle k'^2 \rangle - \langle k' \rangle}{\langle k' \rangle}
= (1-q)\,\kappa_0,
\]
recovering the linear threshold condition $(1-q)\kappa_0 = 1$ stated above.

While $\kappa(q)$ evolves linearly in $q$, $\tilde{P}(k';\,q)$ undergoes nonlinear
reshaping: the tail contracts at rate $(1-q)^{k'}$, faster for large $k'$.
Substantial connectivity redistribution can thus occur while moments change only gradually.
Under random failure the tail erodes slowly; under targeted removal it distorts
immediately. Both processes can accumulate while $S(q)$ remains largely unchanged.
KL divergence, comparing full successive distributions, detects this redistribution where
moment-based indicators miss it. Our objective is not to predict $q_c$ but to detect the
onset of reorganization~\citep{radicchi2024robustness}.

For $q < q_c$, $\tilde{P}(k';\,q)$ is smooth (Eq.~\ref{eq:binomial_thinning}); for small $\Delta q$ (away from $q_c$, where tail contraction eventually invalidates the smooth-derivative assumption),
\begin{equation}
D_{\mathrm{KL}}\!\left(\tilde{P}_{q+\Delta q}\,\|\,\tilde{P}_q\right)
\;\approx\;
\frac{(\Delta q)^2}{2}\,\mathcal{I}(q),
\qquad
\mathcal{I}(q)
=
\sum_k
\frac{\bigl[\partial\tilde{P}(k;\,q)/\partial q\bigr]^2}{\tilde{P}(k;\,q)},
\label{eq:fisher_approx}
\end{equation}
where $\mathcal{I}(q)$ is the Fisher information of $\tilde{P}$ with respect to $q$.
Near $q_c$, accelerated tail contraction drives $\mathcal{I}(q)$ upward, producing the
signal acceleration the detection rule captures. Under targeted failure, $\mathcal{I}(0)$
is already large; no calibration window forms.
The stability of the calibration window $q\le0.15$ in the empirical CL signal is an
empirical property visible in \autoref{fig:random_rep} and confirmed across window
choices in \autoref{tab:baseline_window_sensitivity}; stationarity here means no
sustained exceedances of $\mu_0+2\sigma_0$ within the window at the network scales and
degree exponents studied, not negligible absolute variance.

\subsection{Network Models}

We use Chung--Lu power-law graphs ($P(k)\propto k^{-\gamma}$, $2<\gamma<3$), a regime
characteristic of infrastructure and communication networks, with $N=10{,}000$ nodes fixed
across all synthetic experiments ($N$-dependence discussed in the Discussion). Unlike CM,
which can exhibit degree correlations as a finite-size artifact, CL assigns edges
independently, motivating its use for the primary analysis.

We sweep $\gamma \in \{2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.8, 2.9\}$, averaging over
40 seeds per $\gamma$. We also analyze an empirical CAIDA AS graph and repeat the pipeline
on an exponent-matched CM ensemble (construction in the Appendix;
results in \S\ref{sec:config_model_check_results}).

\subsection{Failure Models}

Two failure regimes: (1) random, nodes removed uniformly at random; (2) targeted,
nodes removed in descending degree order. In both, $q \in [0, 0.9]$ of nodes are removed
and observables computed on $G_q$, implemented using NetworkX~\citep{SciPyProceedings_11}.

\subsection{Structural Signal: KL Divergence and Smoothing}

All signals derive from the empirical degree distribution
\[
P_q(k) = \frac{1}{|V(G_q)|} \sum_{v \in V(G_q)} \mathbf{1}\{\deg(v) = k\}.
\]
The successive KL divergence between adjacent levels is
\[
D_{\mathrm{KL}}\!\left(P_{q+\Delta q} \,\|\, P_q\right)
=
\sum_k
P_{q+\Delta q}(k)
\log_2
\frac{P_{q+\Delta q}(k)}{P_q(k)}.
\]
Large values indicate rapid reorganization; small values indicate stationary evolution.
Evaluated on a finite grid and EWMA-smoothed, we interpret it as a per-step change-rate,
not a continuous-time derivative. We also define degree entropy
\[
H(q) = -\sum_k P_q(k)\log_2 P_q(k),
\]
which summarizes heterogeneity at a fixed state. KL divergence detects reorganization even when $H(q)$ evolves gradually; entropy serves as a complementary indicator only.

\subsubsection*{Secondary control signals}
In the Controls section we also evaluate: (i) successive JS divergence~\citep{lin1991js},
\[
D_{\mathrm{JS}}(P_{q+\Delta q},P_q)=\tfrac12 D_{\mathrm{KL}}(P_{q+\Delta q}\|M)+\tfrac12
D_{\mathrm{KL}}(P_q\|M),
\qquad
M=\tfrac12(P_{q+\Delta q}+P_q),
\]
and (ii) absolute entropy change,
\[
|\Delta H(q)| = \big|H(q+\Delta q)-H(q)\big|.
\]
These assess specificity only.

We leave ordering-aware distances (e.g., Wasserstein on degree) for future comparator
extensions.

\subsubsection*{Moment-based comparator (Molloy--Reed branching factor)}
We also compute $\kappa(q)=(\langle k^2\rangle_q-\langle k\rangle_q)/\langle k\rangle_q$
from empirical $P_q(k)$, with the same EWMA and detection rule; full results are reported in the Appendix.

\subsubsection*{Support and numerical stability}
Degree distributions are evaluated on a fixed discrete support with additive
regularization $\varepsilon = 10^{-12}$ before normalization. EWMA smoothing
suppresses finite-size fluctuations:
\[
\tilde{D}_{\mathrm{KL}}(q_i)
=
\alpha D_{\mathrm{KL}}(q_i)
+
(1-\alpha)\tilde{D}_{\mathrm{KL}}(q_{i-1}).
\]
$\alpha=0.2$ is fixed across all experiments; sensitivity to $\alpha$ and $z$ is in
the Supplementary Material. The smoothed signal $\tilde{D}_{\mathrm{KL}}(q)$ is
the sole warning observable; alternatives appear only in controls.

\subsection{Decision Rules}
\phantomsection
\label{sec:early_warning_criteria}
Two regime-appropriate rules apply to the same signal.

\subsubsection{Random Failure: Baseline Deviation Rule}
Under random failure, $\tilde{D}_{\mathrm{KL}}(q)$ settles within the calibration window
$q \le 0.15$ without sustained exceedance of $\mu_0+2\sigma_0$; this non-triggering
property is confirmed by 0/40 false triggers in the null control
(\autoref{tab:null_control_random_failure}). We estimate $\mu_0$ and $\sigma_0$ over
the window $q \le 0.15$,
which contains $\approx16$ midpoints on the 100-step grid (step size $\Delta q=0.009$,
midpoints at $(i-0.5)\cdot0.009$ for $i=1,\ldots,16$), sufficient for a stable
noise-floor estimate and narrow enough to precede departures at all tested $\gamma$.
Stationarity means no sustained exceedances of $\mu_0 + 2\sigma_0$ within the window,
confirmed by 0/40 null-control false triggers (\autoref{tab:null_control_random_failure}).
The window is fixed across all $\gamma$ and not tuned post hoc; sensitivity to $\alpha$
and $z$ is in the Supplementary Material; sensitivity to the choice of $q_0$
is in \autoref{tab:baseline_window_sensitivity} (\S\ref{sec:baseline_window_sensitivity}).

The early-warning point $q_{\mathrm{warn}}$ is defined as the smallest $q$ such that
\[
\tilde{D}_{\mathrm{KL}}(q) > \mu_0 + 2\sigma_0.
\]

Non-detections are recorded as NaN; detection rates $[n_{\mathrm{det}}/n]$ and
median [IQR] are reported over seeds where $q_{\mathrm{warn}}$ is defined.

\subsubsection{Targeted Failure: Intensity Summary (no timing estimand)}
Under targeted removal, disruption begins immediately; the signal does not admit a
calibration window. KL is evaluated at midpoints, so the earliest reportable value is
$q_{1/2}=\Delta q/2$, at which the response triggers; timing is not a meaningful estimand.
We summarize targeted disruption by the initial shock magnitude
\[
I_{\mathrm{tgt}} := \tilde{D}_{\mathrm{KL}}(q_{1/2}),
\]
reported with $q_{\mathrm{collapse}}$ (defined by $S(q)<0.1$), avoiding
overinterpretation of timing. The collapse proxy is post hoc only; changing its threshold
affects only reported lead times.

\subsection{Experimental Design}
Random failure uses a 100-point grid over $[0,0.9]$; targeted removal uses 400 points.
Each configuration is repeated across 40 seeds. Each seed generates an independent Chung--Lu
graph realization and an independent random node-removal order, so the ensemble averages
over both network heterogeneity and removal stochasticity.
CAIDA experiments use $q_{\max}=0.99$ with 200 points.

We use the CAIDA AS Relationships Dataset (serial-2; snapshot
\CAIDASnapshot)~\citep{caida_as_rel_20260101}. We parse
\texttt{AS1|AS2|rel|source}, drop comment lines, and form an undirected simple graph from
unique AS pairs.

Three network classes: (1) \textbf{Chung--Lu} (primary), $\gamma \in (2,3)$, both
failure regimes, reporting $q_{\mathrm{warn}}$ and $I_{\mathrm{tgt}}$; (2)
\textbf{CAIDA AS graph} (\CAIDASnapshot), out-of-sample realism check; (3)
\textbf{configuration-model} ensembles: (a) exponent-matched (same $\gamma$, fresh degree
sequence) and (b) sequence-matched (exact realized CL degree sequence), isolating the
hard-constraint wiring mechanism. Unless labeled otherwise, all figures and tables refer
to Chung--Lu.

\section{Results}

\subsection{Signal Morphology: Illustrative Single Realization}

% --- Block A: Synthetic random representative (Fig. 1) ---
\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{%
paper/figures/fig1_random_gamma2.5_seed18_alpha0.20.pdf}
\caption{Random failure ($\gamma=2.5$, seed 18, $\alpha=0.20$).
GCC fraction $S(q)$ (left axis) declines gradually while smoothed successive KL
$\tilde{D}_{\mathrm{KL}}(q)$ (right axis) departs from the calibration window well before collapse.
Baseline window $q \le 0.15$ shaded; vertical lines mark $q_{\mathrm{warn}}=0.350$ and
$q_{\mathrm{collapse}}=0.836$. The early-window rise in $\tilde{D}_{\mathrm{KL}}$ reflects an EWMA initialization
transient; the signal settles within the calibration window, and no exceedance of
$\mu_0+2\sigma_0$ occurs within it (0/40 seeds,
\autoref{tab:null_control_random_failure}).}
\label{fig:random_rep}
\end{figure}

\autoref{fig:random_rep} shows the typical structure: calibration window ($q \le 0.15$),
then sustained departure before $q_{\mathrm{collapse}}$; \autoref{fig:gamma_sweep_random}
confirms $q_{\mathrm{warn}} \ll q_{\mathrm{collapse}}$ across all or nearly all seeds
at every $\gamma$ (39--40/40; see \autoref{tab:gamma_sweep_random}). $\kappa(q)$ under the same rule does not trigger in this run or across seeds
(Supplementary Material, S3).

\autoref{fig:kl_decomp} decomposes $D_{\mathrm{KL}}$ into the per-degree contribution
$c(k;\,q)$ (defined in the caption of \autoref{fig:kl_decomp})
across four snapshots spanning baseline, pre-warning, and post-warning regimes.
Contributions are not concentrated at high-degree nodes; both low- and intermediate-degree classes contribute at all damage levels, confirming that the signal reflects broad redistributional change rather than hub-dominated reorganization.
Past $q_{\mathrm{warn}}$, intermediate and high-degree contributions grow, consistent
with progressive hub-connectivity restructuring.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.98\linewidth]{%
paper/figures/fig_kl_decomp_by_k_gamma2.5_seed0_alpha0.20.pdf}
\caption{Per-degree contributions $c(k;\,q) = P(k;\,q{+}\Delta q)\log_2[P(k;\,q{+}\Delta q)/P(k;\,q)]$
at four snapshot steps ($\gamma=2.5$, seed 0, $\alpha=0.20$, random failure), plotted on a log degree axis.
Snapshot $q$ values span the baseline, pre-warning, post-warning, and late-damage regimes
(see \autoref{fig:random_rep} for signal context). Contributions are not concentrated at high-degree nodes; both low- and intermediate-degree classes contribute at all damage levels, confirming that the signal reflects broad redistributional change rather than hub-dominated reorganization.}
\label{fig:kl_decomp}
\end{figure}

% --- Block D/E: γ-sweep summary plot (Fig. 4) + Tables (Tables 1–3) ---
\subsection{Primary Result: 40-Seed Ensemble across the $\gamma$ Sweep}

For each $\gamma$ we run 40 seeds under the fixed baseline-deviation rule. The key claim,
$q_{\mathrm{warn}} \ll q_{\mathrm{collapse}}$ across the heavy-tailed regime, rests on
this ensemble, not any single realization.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{paper/figures/fig_gamma_sweep_random.pdf}
\caption{Random failure: mean $\pm$ standard deviation of $q_{\mathrm{warn}}$ and
$q_{\mathrm{collapse}}$ across 40 seeds as a function of $\gamma$
(Chung--Lu, $N=10{,}000$, $\alpha=0.20$).
$q_{\mathrm{warn}}$ is detected in all or nearly all seeds at every $\gamma$
(see \autoref{tab:gamma_sweep_random} for per-$\gamma$ detection counts and median [IQR]).
The gap $q_{\mathrm{collapse}} - q_{\mathrm{warn}}$ is substantial and consistent across
the entire $\gamma$ range tested.}
\label{fig:gamma_sweep_random}
\end{figure}

\autoref{fig:gamma_sweep_random} and \autoref{tab:gamma_sweep_random} confirm
$q_{\mathrm{warn}} \ll q_{\mathrm{collapse}}$ at every $\gamma$. At $\gamma=2.5$:
40/40 seeds, median lead time $\Delta_{\mathrm{warn}} \approx 0.55$. Under targeted
removal, disruption is immediate at every $\gamma$.

\FloatBarrier
\input{paper/tables/gamma_sweep_random.tex}
\input{paper/tables/gamma_sweep_targeted.tex}
$I_{\mathrm{tgt}}$ decreases monotonically with $\gamma$: lighter tails at high $\gamma$
mean weaker hub-dominated disruption.
\FloatBarrier

\subsection{Rolling EWS Comparators}
\label{sec:ews_comparators}
Two rolling indicators, both under the identical detection rule
($\alpha=0.20$, $z=2$, $q_0\le0.15$)~\citep{scheffer2009earlywarning,dakos2012methods}:
\begin{itemize}
    \item \textbf{Rolling standard deviation} $\mathrm{sd}_w(D_{\mathrm{KL}})$: the sample
    standard deviation of the raw successive KL values within a sliding window of $w=7$
    steps ($\Delta q \approx 0.063$ of damage fraction). Rising variance is a classical
    signature of critical fluctuations near a tipping point~\citep{scheffer2009earlywarning}.
    \item \textbf{Rolling lag-1 autocorrelation} $\mathrm{AC}(1)_w(D_{\mathrm{KL}})$: the
    Pearson correlation between successive KL values at lag 1 within the same sliding window.
    Rising autocorrelation (critical slowing down) is a second standard EWS
    indicator~\citep{dakos2012methods,vannes2007slow}.
\end{itemize}
\autoref{tab:ews_comparison} summarizes detection rates and median $q_{\mathrm{warn}}$
[IQR] across 40 seeds for $\gamma \in \{2.5, 2.7, 3.0\}$. $\gamma=3.0$ is included here
as an out-of-range test point (beyond the main sweep of $\gamma\in(2,3)$) to probe
comparator behavior near the lighter-tail boundary; it is absent from the primary CL
ensemble tables, targeted removal, and noise-floor comparison, which are restricted
to $\gamma\in\{2.1,\ldots,2.9\}$.
Rolling-sd matches detection rate (40/40 at $\gamma=2.5$, 39/40 at $\gamma=3.0$) but
$q_{\mathrm{warn}}$ is $\approx0.013$--$0.023$ later, reflecting rolling-window lag.
Rolling-AC(1): 22/40 ($\gamma=2.5$), 15/40 ($\gamma=2.7$), 18/40 ($\gamma=3.0$), and
consistently later $q_{\mathrm{warn}}$. The low AC(1) rate reflects the short effective
memory of the KL sequence relative to the rolling window. Direct baseline deviation on
$D_{\mathrm{KL}}$ outperforms both rolling-window alternatives on timing and consistency.

\input{paper/tables/ews_comparison.tex}

\subsection{Fisher Information Shape: Mean-Field Reference}
\label{sec:analytical_kl}

\autoref{fig:fisher_info} shows $\mathcal{I}(q)$, computed numerically from
$\tilde{P}(k';\,q)$ (Eq.~\ref{eq:binomial_thinning}) via central finite differences,
for $\gamma\in\{2.1,2.5,2.9\}$ on a log scale.

$\mathcal{I}(q)$ is large at small $q$, declines through a mid-damage minimum near
$q\approx0.4$--$0.5$, then grows again toward $q_c$; successive KL per step scales as
$(\Delta q)^2\,\mathcal{I}(q)/2$, so the post-minimum rise in successive KL remains small
in absolute terms. For $\gamma=2.1$ and $\gamma=2.5$, $q_c$ lies beyond $q_{\max}=0.9$
and the near-$q_c$ uptick is not visible in the figure; for $\gamma=2.9$, $q_c\approx0.80$
falls within the plotted range. Here $q_c$ denotes the Molloy--Reed percolation threshold
applied to the population-level residual distribution $\tilde{P}(k';\,q)$; the empirical
collapse fraction $q_{\mathrm{collapse}}$ (defined by $S(q)<0.1$ in finite graphs)
differs from this analytical threshold due to finite-size effects and graph
stochasticity---see \autoref{tab:gamma_sweep_random} for empirical values.

Under the baseline-deviation rule applied to the analytical signal, detection does not
occur: the calibration window falls in the elevated high-$\mathcal{I}(q)$ early regime,
so $\mu_0$ is estimated from a high-amplitude region and the threshold sits above the
post-window signal (see Discussion, \S\ref{sec:discussion_noise_floor}).

The calibration window $q \le 0.15$ falls in the high-$\mathcal{I}(q)$ early regime.
In the empirical CL signal, the calibration window exhibits lower amplitude and variance than
this analytical early regime, because finite-sample degree distributions produce noise
floors lower than the elevated population-level early region. Near $q_c$, cascade dynamics
produce correlated shifts absent from binomial thinning, lifting the empirical signal above
the mean-field prediction. The post-minimum monotone rise in $\mathcal{I}(q)$ confirms
that distributional change accelerates approaching $q_c$. The analytical-to-empirical
mismatch identifies cascade dynamics as the operative mechanism.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\linewidth]{paper/figures/fig_fisher_info.pdf}
\caption{Fisher information $\mathcal{I}(q)$ of the residual degree distribution
$\tilde{P}(k';\,q)$ (Eq.~\ref{eq:binomial_thinning}), computed via central finite
differences, for $\gamma\in\{2.1,2.5,2.9\}$ (log $y$-axis).
Shaded: calibration window ($q \le 0.15$); gray dotted vertical line: analytical
Molloy--Reed threshold $q_c$ for $\gamma=2.9$ (the only case with $q_c < q_{\max}=0.9$).
Note that $q_c$ here is the population-level percolation threshold; the empirical
collapse fraction $q_{\mathrm{collapse}}$ in finite $N=10{,}000$ graphs is typically
lower (see \autoref{tab:gamma_sweep_random}).
$\mathcal{I}(q)$ is large at small $q$, reaches a minimum in the mid-damage range, then
grows toward $q_c$, confirming that distributional change accelerates as the percolation
threshold is approached.
For $\gamma=2.9$, where $q_c$ falls within the plotted range, the uptick near $q_c$ is
clearly visible; for $\gamma=2.1$ and $\gamma=2.5$, $q_c$ lies beyond $q_{\max}=0.9$
and the growth phase is not fully visible.}
\label{fig:fisher_info}
\end{figure}

% --- Block C: Synthetic targeted representative (Fig. 2) ---
\subsection{Targeted Failure: Immediate Disruption}

Under hub-first removal, disruption begins at the first step; onset timing is not a
meaningful estimand. We summarize by $I_{\mathrm{tgt}}:=\tilde{D}_{\mathrm{KL}}(q_{1/2})$
and $q_{\mathrm{collapse}}$; refining the grid moves $q_{1/2}$ toward zero without
changing the interpretation.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{%
paper/figures/fig2_targeted_gamma2.5_seed0_alpha0.20.pdf}
\caption{Targeted (hub-first) failure ($\gamma = 2.5$, seed 0, $\alpha = 0.20$).
$S(q)$ collapses abruptly at $q_{\mathrm{collapse}} = 0.091$; the successive-KL signal
jumps at the first midpoint $q_{1/2} = \Delta q / 2$ (gold dashed line). Note that this
seed's $q_{\mathrm{collapse}}=0.091$ lies slightly above the ensemble median
(\autoref{tab:gamma_sweep_targeted}); the qualitative pattern---immediate disruption at
the first step---holds uniformly across seeds.
\autoref{tab:gamma_sweep_targeted} reports $q_{\mathrm{collapse}}$ and $I_{\mathrm{tgt}}$
across 40 seeds and the full $\gamma$ sweep.}
\label{fig:targeted_rep}
\end{figure}

\autoref{tab:gamma_sweep_targeted} shows $q_{\mathrm{collapse}}\approx 0.07$--$0.09$
across the $\gamma$ sweep.

% --- Block B: Empirical CAIDA random failure (Fig. 3) ---
\subsection{Empirical Validation: CAIDA AS Graph}
\autoref{fig:caida_random} shows a representative run;
\autoref{tab:caida_summary} reports the 40-seed summary.

\begin{figure}[htbp]
\centering
\input{paper/figures/caida_random_constants_20260101_seed0.tex}
\begin{tikzpicture}
\pgfplotsset{every axis legend/.append style={legend columns=2, draw=none, /tikz/column
sep=1.6ex}}
\begin{axis}[
    name=caidaS,
    width=0.95\linewidth,
    height=6cm,
    xmin=0, xmax=0.995,
    ymin=0, ymax=1,
    xlabel={$q$},
    ylabel={$S(q)$},
    yticklabel style={text=black},
    y label style={text=black},
    grid=both,
    grid style={opacity=0.2},
    legend to name=caidaLegend,
]
% Baseline window q <= 0.15 (shaded)
\addplot [draw=none, fill=black, fill opacity=0.12, forget plot] coordinates {(0,0)
(0.15,0) (0.15,1) (0,1)}
\closedcycle;
\addplot [black!25, very thin, forget plot] coordinates {(0.15,0) (0.15,1)};
\addplot [black, very thick] table[x=q, y=S]
{paper/figures/caida_random_S_20260101_seed0.dat};
\addlegendentry{$S(q)$}
% Legend entry for dKL should use the actual plotted style from the right axis (no manual
% colors).
\addlegendimage{/pgfplots/refstyle=caida:dkl}
\addlegendentry{$\tilde{D}_{\mathrm{KL}}(q)$}
% Warning marker
\addplot [orange, dashed, thick, forget plot] coordinates {(\CAIDAQWarn,0) (\CAIDAQWarn,1)};
% Collapse marker
\addplot [black, dotted, very thin, opacity=0.7, forget plot] coordinates {(\CAIDAQCollapse,0)
(\CAIDAQCollapse,1)};
\end{axis}

\begin{axis}[
    width=0.95\linewidth,
    height=6cm,
    xmin=0, xmax=0.995,
    axis x line=none,
    axis y line*=right,
    ylabel={$\tilde{D}_{\mathrm{KL}}(q)$},
    yticklabel style={color=purple},
    y label style={color=purple},
    at={(caidaS.south west)},
    anchor=south west,
]
\addplot [purple, very thick] table[x=q_mid, y=dkl_smooth]
{paper/figures/caida_random_dkl_20260101_seed0_alpha0.20.dat};
\label{caida:dkl}
\end{axis}
\node[anchor=south, yshift=-2pt] at (caidaS.north) {\pgfplotslegendfromname{caidaLegend}};
\end{tikzpicture}
\caption{CAIDA AS graph (2026-01-01, $N=78{,}673$ nodes), random failure (seed 0, $\alpha = 0.20$). GCC fraction $S(q)$ (left axis) and smoothed successive KL $\tilde{D}_{\mathrm{KL}}(q)$ (right axis); calibration window $q \leq 0.15$ shaded. The baseline-deviation rule identifies $q_{\mathrm{warn}} \approx 0.689$ ahead of $q_{\mathrm{collapse}} \approx 0.985$; 40-seed summary in \autoref{tab:caida_summary}.}
\label{fig:caida_random}
\end{figure}
CAIDA replicates the calibration-window-then-departure pattern (40/40, median
$\Delta_{\mathrm{warn}} \approx 0.24$; \autoref{tab:caida_summary}).

\input{paper/tables/caida_summary.tex}

\subsection{Ensemble Sensitivity: Configuration-Model Experiments}
\label{sec:config_model_check_results}
We repeat the $\gamma$ sweep on an exponent-matched CM ensemble (construction in
Supplementary Material S4; all settings identical). Detection drops from
357/360 (CL) to 68/360 (CM) (\autoref{tab:config_random}): the signal is sensitive to
ensemble structure beyond the tail exponent.

\input{paper/tables/gamma_sweep_random_config.tex}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\linewidth]{paper/figures/kl_signal_comparison.pdf}
\caption{Smoothed successive KL $\tilde{D}_{\mathrm{KL}}(q)$ under random failure for a
Chung--Lu network (left) and an exponent-matched configuration-model network (right),
$\gamma=2.5$, seed 0, $\alpha=0.20$.
Shaded: calibration window $q\le 0.15$; horizontal dashed: detection threshold $\mu_0+2\sigma_0$;
vertical dashed (left only): $q_{\mathrm{warn}}$.
CL triggers; in CM, stub-pairing randomness elevates the baseline noise floor, raising the detection threshold above the post-window signal and revealing the indicator's sensitivity to hard-constraint wiring.}
\label{fig:kl_signal_comparison}
\end{figure}

The wiring mechanism determines whether detection transfers: in CL, expected-degree wiring produces smooth histogram drift that accumulates into a
sustained departure. In CM, hard-constraint wiring produces churn even at $q\le0.15$,
inflating $(\mu_0,\sigma_0)$ and raising the threshold above the post-window signal.
The indicator does not trigger in the majority of CM runs not because the signal is absent
but because stub-pairing randomness elevates the noise floor: in 276/292 CM runs without a
warning, the post-window signal never exceeded $\mu_0+2\sigma_0$, a consequence of low SNR
that reflects the indicator's sensitivity to hard-constraint structural correlations.

\input{paper/tables/baseline_noise_comparison.tex}

For ensembles with known properties, a pre-calibrated threshold would restore
transferability; characterizing which structural properties govern ensemble-stable transfer
is a natural next step.

\subsection{Controls and Specificity}
\label{sec:controls}
% --- Block F: Control analyses (no-damage null and low-damage) ---
We also benchmark against $\kappa(q)$ (Supplementary Material S3).
\subsubsection*{Null control}
We run the full pipeline with $G_q \equiv G_0$ (no removals). All five signals produce
0/40 false triggers (\autoref{tab:null_control_random_failure}): finite-size resampling
noise alone cannot cause threshold exceedance.

\input{paper/null_control_random_failure_gamma2.5_alpha0.20.tex}

\subsubsection*{Damage-without-collapse control}
We restrict to $q\le 0.25$ (far from collapse, $\min S=0.653$) and apply the same rule
(\autoref{tab:control_low_damage}). Successive KL: 0/40; JS: 3/40; $|\Delta H|$: 37/40;
rolling-sd: 27/40; rolling-AC(1): 10/40. Rolling-sd and $|\Delta H|$ lack specificity.
$\kappa(q)$: 0/40, $Z_{\max}^{\kappa}=1.22\pm0.28$ (weak signal, distinct from CM
threshold inflation; see Supplementary Material S3).

\input{paper/tables/control_low_damage_gamma2.5_alpha0.20.tex}

\subsection{Degree-Matched Configuration-Model Diagnostic}
\label{sec:degree_matched_config}
The existing configuration-model experiment (\nameref{sec:config_model_check_results}) matches
$\gamma$ between the Chung--Lu source and the configuration-model network, but uses an
independently sampled power-law degree sequence for each CM run.
To sharpen the mechanistic attribution, we introduce a degree-matched variant:
for each (gamma, CL seed) pair we (1) generate a Chung--Lu graph $G_{\mathrm{CL}}$ and extract
its realized degree sequence $\mathbf{d}$, then (2) construct 5 CM realizations from
that exact degree sequence, varying only the stub-pairing randomness.
With 10 CL seeds per $\gamma$, this yields 50 sequence-matched CM runs per $\gamma$.
This controls for the degree sequence itself; any residual detection difference isolates the
effect of the hard-constraint wiring mechanism from distributional properties of $\mathbf{d}$.

\autoref{tab:degree_matched_config} summarizes detection rates across $\gamma$ values.
Detection rates in the sequence-matched CM are intermediate between those of the CL ensemble
(40/40 at all tested $\gamma$) and the exponent-matched CM ($\approx19$\% detection rate
from the main ensemble experiment): at $\gamma=2.5$, 28/50 sequence-matched CM runs
produce a valid warning, compared to $\approx$4/40 in the exponent-matched CM.
This pattern implies that both the degree sequence and the wiring mechanism
contribute to detection behavior: when the degree sequence is drawn from the same CL
source, detection improves substantially, but the hard-constraint wiring still elevates the noise
floor relative to CL. The residual gap ($28/50$ vs $40/40$) confirms that stub-pairing
randomness, independent of the degree sequence, is a distinct structural factor shaping
the indicator's behavior.

Together, the exponent-matched and sequence-matched experiments point to two separable
channels of ensemble sensitivity: (i) the degree sequence shape (the exponent-matched CM
uses a fresh power-law sequence, which introduces additional structural churn), and
(ii) the hard-constraint wiring mechanism itself (which generates calibration-window churn
even when the degree sequence is fixed to match CL).

\input{paper/tables/degree_matched_config_random_failure.tex}

\subsection{Sensitivity Analysis: Baseline Window}
\label{sec:baseline_window_sensitivity}
The baseline-deviation rule depends on the choice of calibration window endpoint $q_0$.
We sweep $q_0 \in \{0.10, 0.15, 0.20, 0.25\}$ on the primary Chung--Lu random-failure
analysis ($\alpha=0.20$, $z=2$, 40 seeds per $\gamma$), re-applying the detection rule to
the same smoothed signal with each window boundary.
\autoref{tab:baseline_window_sensitivity} shows detection rates and $q_{\mathrm{warn}}$
statistics for each $q_0$ across three $\gamma$ values.
At $q_0=0.10$ and $q_0=0.15$, detection rates are 40/40 or 39/40 for all tested $\gamma$
values. Detection rates decline slightly at $q_0=0.20$ and $q_0=0.25$ (to 35--37/40),
as expected: a wider calibration window leaves a smaller post-window region in which an
exceedance can be counted, and the estimated noise floor is computed from more data
points, which reduces variance in the threshold.
Median $q_{\mathrm{warn}}$ shifts upward with increasing $q_0$ by construction, since
the search region begins later. The detection rate itself, however, remains high across
the full tested range, confirming that the signal is not an artifact of the $q_0=0.15$
default choice.

\input{paper/tables/baseline_window_sensitivity.tex}

\section{Discussion}
\label{sec:discussion_noise_floor}

Across Chung--Lu power-law graphs with $2<\gamma<3$, EWMA-smoothed successive KL divergence
between adjacent empirical degree distributions departs from baseline after the calibration
window before GCC collapse under random node removal, yielding $q_{\mathrm{warn}} \ll
q_{\mathrm{collapse}}$ in all or nearly all seeds at every tested $\gamma$ and on the CAIDA
AS graph. Under targeted (hub-first) removal, no calibration window exists; we instead record
initial shock magnitude $I_{\mathrm{tgt}} := \tilde{D}_{\mathrm{KL}}(q_{1/2})$. Controls
confirm specificity: successive KL produces zero false triggers in null and low-damage regimes
where entropy-change and rolling-sd signals trigger frequently (37/40 and 27/40). It fires
earlier than rolling-sd EWS by ${\approx}0.02$ in median $q_{\mathrm{warn}}$ and more
consistently than rolling AC(1) (40/40 vs. 15--22/40), while moment-based $\kappa(q)$
produces zero detections in the low-damage control (\autoref{tab:control_low_damage};
full-sweep zero-detection results in Supplementary Material S3), confirming that the full
distributional signal is necessary.
The per-degree decomposition (Figure~\ref{fig:kl_decomp}) shows contributions distributed
across degree classes, not concentrated at high-degree nodes. A direct comparison between
$D_{\mathrm{KL}}(q)$ and the mean degree of nodes removed at each step remains future work.

Pre-collapse signal acceleration is explained analytically via binomial thinning: the
Fisher information $\mathcal{I}(q)$ of the residual distribution grows as the high-degree
tail contracts near the percolation threshold, producing $(\Delta q)^2\,\mathcal{I}(q)$
per-step KL that accelerates approaching $q_c$, matching the observed post-window rise.
The stability of the calibration window is an empirical property of the finite-sample CL
signal, visible in \autoref{fig:random_rep} and robust to window-endpoint choice
(\autoref{tab:baseline_window_sensitivity}).

The indicator's divergence between Chung--Lu and the configuration model reveals the
indicator's sensitivity to localized structural correlations and finite-size stub-matching
constraints that mean-field models ignore.
The dominant structural property governing ensemble transfer is the baseline KL noise floor.
In Chung--Lu graphs, the expected-degree wiring (soft degree constraint) produces
smooth, low-amplitude step-to-step variation in $P_q(k)$: at $\gamma=2.5$,
$\mu_0=0.058\pm0.007$ and $\sigma_0=0.011\pm0.003$ (mean $\pm$ SD, 40 seeds), so the
threshold $\mu_0+2\sigma_0=0.080$ sits below the post-window maximum ($0.106\pm0.017$)
in all 40 seeds. Configuration-model graphs (hard degree constraint) exhibit substantially
higher baseline noise: $\mu_0=0.118\pm0.016$ and $\sigma_0=0.032\pm0.014$, driving the
threshold to $0.182\pm0.042$, which exceeds the post-window maximum ($0.134\pm0.020$) in
31 of 40 seeds (the remaining 5 non-detecting seeds had threshold below the post-window
maximum but no sustained exceedance, for a total of 36/40 non-detecting seeds). The mechanistic source is stub-pairing randomness: surviving nodes' stubs
are re-paired randomly at each damage step, introducing per-step variability in $P_q(k)$
absent in CL. This pattern holds across $\gamma$: threshold exceeds post-window maximum
in 26--36 of 40 CM runs for every $\gamma\in\{2.1,\ldots,2.9\}$, and in 0/40 CL runs.

$\mathcal{I}(q)$ computed from binomial thinning (\autoref{fig:fisher_info}) is large at
small $q$, reaches a minimum in the mid-damage range, then grows toward $q_c$; since
successive KL per step scales as $(\Delta q)^2\mathcal{I}(q)/2$, the post-minimum rise
remains small in absolute terms. The calibration window $q\le0.15$ falls on the
high-$\mathcal{I}(q)$ early portion of the curve, meaning the population-level analytical
signal does not itself exhibit a calibration window with low-noise behavior; the
non-triggering property observed in the CL ensemble is a finite-sample property, not a
consequence of population-level distributional dynamics.
Under the same baseline-deviation rule, this analytical signal does not trigger: the
calibration window $q \le 0.15$ falls on the elevated early portion of the signal, so
$\mu_0$ and $\sigma_0$ are estimated from a high-amplitude region, placing the threshold
above the post-window signal. Empirical detection in the Chung--Lu ensemble succeeds
because finite-sample degree distributions produce noise floors lower and flatter than the
elevated analytical early region, giving the post-window rise room to exceed the threshold;
detection is thus a property of finite-sample signal geometry, not population-level dynamics
alone. The mechanism underlying this geometry is the simultaneous stub deletion absent from
the binomial-thinning model (Eq.~\ref{eq:binomial_thinning}): that model assumes each edge
survives independently with probability $(1-q)$, whereas actual node removal deletes all
edges incident to a removed node at once, inducing correlated degree changes across the
entire neighborhood and introducing structural cascades that the mean-field independence
assumption cannot capture. These cascade-induced correlations keep the empirical
baseline noise floor lower and flatter than the elevated analytical early region while
concentrating distributional shift into the post-window signal. This discrepancy between
the mean-field analytical prediction and the empirical signal is real and consequential:
empirical detection succeeds specifically because cascade-induced correlations suppress
the baseline noise floor below the population-level prediction; the analytical framework
correctly characterizes population-level distributional dynamics but does not capture
the simultaneous stub deletion that governs empirical baseline geometry. This identifies a scope condition for the baseline-deviation rule: the calibration
window must fall in the low-noise mid-range of the empirical signal rather than the
high-$\mathcal{I}(q)$ early regime, a condition satisfied by $q \le 0.15$ at the network
scales and degree exponents studied here and confirmed by the null and low-damage controls.
This signal shape also suggests that adaptive window placement, selecting from the empirical
mid-range rather than a fixed low-damage interval, could improve robustness across network
sizes and ensemble types.

The SNR contrast itself encodes structural information:
$\mathrm{SNR}=\max_{q>q_0}\tilde{D}_{\mathrm{KL}}(q)\,/\,(\mu_0+z\sigma_0)$ exceeds one
in nearly all CL seeds and falls below one in the majority of CM runs, quantifying exactly
how much of the indicator's dynamic range is consumed by stub-pairing correlations rather
than damage-driven reorganization. Pre-estimating $\mu_0$ and $\sigma_0$ from ensemble
structure or a reference run, rather than from the damage window, is the natural path to
ensemble-stable transfer.

The absence of a KL precursor under targeted hub removal is consistent with Cohen et
al.~\citep{cohen2001breakdown}: scale-free networks with $\gamma \leq 3$ collapse after
eliminating only a vanishingly small fraction of hubs, so the degree distribution is
distorted at the first removal step and no calibration window forms. The result
motivates a clean separation between two monitoring objectives: (i) advance warning of
collapse under gradual random attrition, and (ii) shock quantification under targeted or
adversarial removal via $I_{\mathrm{tgt}}$.

This study intentionally restricts the structural state to the empirical degree distribution
to keep the signal interpretable and the decision rule fixed across regimes; richer summaries
(joint-degree structure, mixing, motifs) may refine warning behavior under adversarial removal.
All synthetic experiments use $N=10{,}000$ nodes; the CAIDA AS graph ($N=78{,}673$) provides one cross-scale data point (40-seed
detection rate 40/40 with qualitatively matched signal shape), though systematic
finite-size analysis remains future work.
Natural next steps are to track change rates in higher-order structural features and to test
analogous rules under link removal and correlated or temporally clustered failure processes.

Reproducibility commands, sensitivity tables, the $\kappa(q)$ benchmark, and configuration-model construction details are provided in the Supplementary Material (submitted as a separate file).

\section*{Declarations}

\subsection*{Ethical standards}
This research did not involve human participants, animal subjects, or data requiring ethical review.

\subsection*{Availability of data and materials}
All code required to reproduce the results in this article is available at \url{https://github.com/7astro7/network-robustness-paper} under the Apache 2.0 license. The CAIDA AS Relationships Dataset must be obtained separately from \url{https://www.caida.org/data/as-relationships/}.

\subsection*{Competing interests}
The author declares no competing interests.

\subsection*{Funding}
No funding was received for this work.

\subsection*{Authors' contributions}
Z.A.K. conceived the study, developed the methodology, implemented all experiments, analyzed the results, and wrote the manuscript.

\subsection*{Acknowledgements}
Not applicable.

\bibliographystyle{apalike}
\bibliography{references}

\end{document}
